# Optional. Without this key, GramBiz still returns deterministic analysis.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5
# Comma-separated frontend origins allowed to call this API.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
